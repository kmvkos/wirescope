[Service]
# Fallback console when the kiosk is disabled or not started.
# While the kiosk runs it Conflicts=getty@tty1.service and owns tty1.
ExecStart=
ExecStart=-/sbin/agetty --autologin wirescope --noclear %I $TERM
