# optional local operator display (not a full desktop)
# generic non-Pi fallback stack
cage
xserver-xorg
xinit
openbox
chromium
xserver-xorg-input-all

# Raspberry Pi system install intentionally selects only:
# cage
# chromium
